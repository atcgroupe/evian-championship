{% extends 'core/core.html.twig' %}

{% block main %}
    <div class="max-w-screen-md mx-auto">
        {# Title #}
        <h1 class="text-3xl font-light text-center my-6">
            <span class="px-2 border border-prunoise rounded-md text-prunoise">
                {{ app.user.displayName('complete') }}
            </span>
        </h1>

        {# Email notifications #}
        {{ include('user/settings/_index_notification.html.twig') }}
    </div>
{% endblock %}
